# 论文精读笔记

## 1. 基本信息

- 标题：未明确提及
- 作者：Ranjan Sapkota、Rahul Harsha Cheppally、Ajay Sharda、Manoj Karkee
- 年份：未明确提及
- 会议/期刊：未明确提及

### 摘要

This study presents a comprehensive analysis of Ultralytics YOLO26, highlighting its key architec-
tural enhancements and performance benchmarking for real-time edge object detection. YOLO26,
released in September 2025, stands as the newest and most advanced member of the YOLO family,
purpose-built to deliver efficiency, accuracy, and deployment readiness on edge and low-power
devices. The paper sequentially details YOLO26’s architectural innovations, including the removal of
Distribution Focal Loss (DFL), adoption of end-to-end NMS-free inference, integration of ProgLoss
and Small-Target-Aware Label Assignment (STAL), and the introduction of the MuSGD optimizer
for stable convergence. Beyond architecture, the study positions YOLO26 as a multi-task framework,
supporting object detection, instance segmentation, pose/keypoints estimation, oriented detection, and
classification. We present performance benchmarks of YOLO26 on edge devices such as NVIDIA
Jetson Nano and Orin, comparing its results with YOLOv8, YOLOv11, YOLOv12, YOLOv13, and
transformer-based detectors. This paper further explores real-time deployment pathways, flexible
export options (ONNX, TensorRT, CoreML, TFLite), and quantization for INT8/FP16. Practical
use cases of YOLO26 across robotics, manufacturing, and IoT are highlighted to demonstrate
cross-industry adaptability. Finally, insights on deployment efficiency and broader implications are
discussed, with future directions for YOLO26 and the YOLO lineage outlined.

## 2. 论文解决的问题

实时边缘端目标检测需要在低功耗设备上同时兼顾精度、速度与部署简洁性；现有YOLO版本仍依赖NMS后处理与DFL损失，带来延迟开销与导出困难，制约了在边缘和低功耗场景下的落地效率。

## 3. 背景与动机

现有YOLO变体（如YOLOv12、YOLOv13）虽在精度上不断进步，但仍沿用NMS和DFL，导致推理流水线复杂、延迟不稳定且不利于低功耗设备部署；同时端侧应用对小目标检测和训练收敛稳定性提出更高需求，因此需要一种面向边缘计算、简化部署流程并提升小目标精度的新一代模型。

## 4. 核心方法

YOLO26是Ultralytics于2025年9月发布的边缘端优化模型，通过去除DFL损失、采用端到端无NMS推理、引入ProgLoss渐进式损失平衡与STAL小目标感知标签分配、以及提出MuSGD优化器（SGD与Muon混合），在简化部署的同时提升训练稳定性和小目标检测精度。模型支持目标检测、实例分割、姿态估计、旋转目标检测和分类五项任务，并在Jetson Nano/Orin等边缘设备上与YOLOv8、YOLOv11、YOLOv12、YOLOv13及Transformer检测器进行性能对比。

## 5. 创新点总结

- 移除DFL损失，将回归头改为单峰直接回归，简化边界框解码并提升导出兼容性
- 实现端到端NMS-free推理，消除后处理瓶颈，缩短流水线时延并降低阈值调参需求
- 设计ProgLoss渐进式损失平衡，自适应重加权各阶段损失，缓解难易样本不平衡
- 提出STAL小目标感知标签分配，针对小目标/遮挡目标优化分配策略，提升小目标召回率
- 提出MuSGD优化器（SGD与Muon混合），兼顾SGD的泛化性与Muon的曲率信息，加速收敛并增强稳定性

## 6. 实验设计与结果

实验章节对YOLO26进行了全面的基准测试与对比分析，覆盖目标检测、实例分割、图像分类、姿态估计和旋转目标检测任务。在COCO检测任务上（表2），YOLO26n至YOLO26x的mAP50-95从40.9%提升至57.5%，并且相比基于Transformer的模型（如RT-DETR、YOLOv10），在更低的CPU（ONNX）和GPU（TensorRT）延迟下实现了高精度，得益于无NMS推理和简化回归设计。实例分割（表2）中，各规模YOLO26-seg模型在保持低计算成本和实时吞吐量的同时，获得了具有竞争力的box和mask mAP。在ImageNet分类（表3）上，Top-1准确率从71.4%（n）上升至79.9%（x），最大模型在TensorRT FP16下推理延迟仍低于4ms。姿态估计（表4）在COCO上，mAPpose从57.2%（n）提升至71.6%（x）。旋转目标检测（表5）在DOTA v1上，以1024px输入，mAPtest从52.4%（n）提升至56.7%（x），小模型GPU延迟低于5ms。总体上，YOLO26展示了跨任务和跨规模的精度-效率平衡，适合边缘和服务器实时部署。

## 7. 方法优点

YOLO26是Ultralytics于2025年9月发布的边缘端优化模型，通过去除DFL损失、采用端到端无NMS推理、引入ProgLoss渐进式损失平衡与STAL小目标感知标签分配、以及提出MuSGD优化器（SGD与Muon混合），在简化部署的同时提升训练稳定性和小目标检测精度。模型支持目标检测、实例分割、姿态估计、旋转目标检测和分类五项任务，并在Jetson Nano/Orin等边缘设备上与YOLOv8、YOLOv11、YOLOv12、YOLOv13及Transformer检测器进行性能对比。

## 8. 方法局限

- 未明确提及

## 9. 对我研究的启发

- 面向边缘和低功耗设备时，可从推理流水线简化（去除NMS/DFL）入手提升部署效率
- 损失函数设计上可通过渐进式权重调整增强训练稳定性
- 标签分配算法可对小目标设置专门策略，是提升小目标性能的有效思路
- 优化器层面的混合策略（如MuSGD）为加速收敛与稳定训练提供新方向
- 多任务统一架构有助于降低开发和部署成本，值得在后续检测器设计中参考
